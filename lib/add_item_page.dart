// * หน้าเพิ่มของหาย
import 'package:flutter/material.dart';

class AddItemPage extends StatelessWidget {
  const AddItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
      body: const Center(
        child: Text('นี่คือหน้าเพิ่มของหาย'),
      ),
    );
  }
}
// * หน้าโปรไฟล์ผู้ใช้
import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
      body: const Center(
        child: Text('นี่คือหน้าโปรไฟล์ผู้ใช้'),
      ),
    );
  }
}
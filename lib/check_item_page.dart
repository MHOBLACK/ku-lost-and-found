// * หน้าตรวจสอบวัตถุจากกล้องมือถือ
import 'package:flutter/material.dart';

class CheckItemPage extends StatelessWidget {
  const CheckItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
      body: const Center(
        child: Text('นี่คือหน้าตรวจสอบวัตถุจากกล้องมือถือ'),
      ),
    );
  }
}